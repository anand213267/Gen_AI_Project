[project]
name = "ai-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "anandlucid", email = "anands@lucidux.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ai-project = "ai_project:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
